RewriteEngine On
RewriteBase /

# Jika file atau folder ada → langsung serve
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Selain itu, arahkan ke index.html
RewriteRule ^ index.html [L]